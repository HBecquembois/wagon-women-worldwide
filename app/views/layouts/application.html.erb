<!DOCTYPE html>
<html>
  <head>

    <title>WagonWomenWorldwide</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/tom-select@2.2.2/dist/css/tom-select.css" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <script src="https://kit.fontawesome.com/45e38e596f.js" crossorigin="anonymous"></script>
    <%= javascript_importmap_tags %>
    <%= favicon_link_tag %>
  </head>

  <body class="<%= "#{controller_name}-#{action_name}" %>">
    <%= render "shared/navbar" if action_name == 'home' %>
    <%= render "shared/navbar2" if action_name != 'home' %>
    <%= render "shared/flashes" %>
    <div class="main-container">
      <%= render "shared/sidebar" if action_name != 'home' && controller_name != 'sessions'%>
      <section class="main-content">
        <%= yield %>
      </section>
    </div>
    <%= render "shared/footer" if action_name == 'home' %>
  </body>
</html>
